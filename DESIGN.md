# 易经八卦映射智能体 · 设计方案

> 本文档供开发时参考，按阶段划分便于 Git 管理与迭代。

---

## 项目目标

1. **验证**：八卦映射在不同卦爻辞中是否语义一致
2. **挖掘**：卦象组合→卦辞、爻位→爻辞的关联模式
3. **应用**：智能体可检索、解释、推荐卦爻辞

---

## 数据概况

| 类型 | 数量 | 说明 |
|------|------|------|
| 八卦映射 | 8 卦 × 7 维度/卦 | 来源：`八卦完整映射空间总表.pdf`，已转为 `data/bagua_mappings/bagua_mappings.json` 与 `八卦完整映射空间总表.md` |
| 卦辞 | 64 条 | 含原文 + 白话 |
| 爻辞 | 386 条 | 含原文 + 白话 |

---

## 阶段划分

### 阶段 0：项目骨架（当前）

**目标**：完成项目初始化，建立基础结构。

**产出**：
- [x] 项目目录、Git 初始化
- [x] uv 包管理（pyproject.toml）
- [x] 本设计文档

**Git**：`feat/phase-0-skeleton`

---

### 阶段 1：映射表 → 智能体可读格式 ✅

**目标**：将现有八卦映射表格转化为「结构化 + 自然语言」双形式，便于智能体解析与 LLM 理解。

**产出**：
- [x] 八卦映射的 JSON 结构化数据（`data/bagua_mappings/bagua_mappings.json`）
- [x] 每卦一段连贯的自然语言描述（供 LLM 直接理解）
- [x] 数据加载与验证脚本（`load-bagua`、`validate-bagua` CLI）

**格式示例**：
```json
{
  "乾": {
    "结构化": {"自然": "天", "人事": "父", "德性": "健", ...},
    "描述": "乾卦象征天、父、刚健，主创生与进取..."
  }
}
```

**目录建议**：`data/bagua_mappings/`

**Git**：`feat/phase-1-mapping-format`

---

### 阶段 2：卦爻辞数据入库 ✅

**目标**：将 64 卦辞、386 爻辞以结构化形式存储，支持动态录入与校验。每卦一单元，卦辞与爻辞同文件。

**存储方式**：每卦一文件，如 `data/hexagrams/01_乾.json`，便于增量录入与 Git diff。

**Schema 示例**：
```json
{
  "元信息": {
    "卦名": "乾",
    "卦序": 1,
    "卦画": "䷀",
    "上下卦": { "上卦": "乾", "上卦数": 1, "下卦": "乾", "下卦数": 1 },
    "别名": ["乾为天", "八纯卦之阳"]
  },
  "卦辞": {
    "原文": "元，亨，利，贞。",
    "白话": "创始、通达、适宜、正固。",
    "彖传": "大哉乾元...",
    "大象传": "天行健，君子以自强不息。",
    "关键词": ["创始", "刚健", "天行", "自强不息"],
    "吉凶标签": "大亨通"
  },
  "爻辞": [
    {
      "爻位": "初九", "爻位序": 1, "阴阳": "阳", "当位": true,
      "原文": "潜龙勿用。", "白话": "龙潜伏在水中，暂不宜有所作为。",
      "小象传": "'潜龙勿用'，阳在下也。", "吉凶标签": "慎守",
      "爻象推导": { "本爻象": "...", "取象": "潜龙", "逻辑": "..." },
      "关联映射": ["乾为龙", "乾为健", "初位为潜藏"],
      "应爻": "九四", "承乘": []
    }
  ]
}
```
彖传、小象传、爻象推导、关联映射、承乘等允许 `null` 或空数组，支持逐步完善。乾卦可含「用九」、坤卦可含「用六」作为第 7 条爻辞（爻位序为 0，当位为 null）。

**落地步骤**：
1. 创建 `data/hexagrams/` 目录，添加 `01_乾.json` 作为模板
2. 实现 `scripts/add_hexagram.py`：交互式录入，按 schema 写入对应 JSON 文件
3. 实现 `scripts/validate_hexagrams.py`：校验必填字段、卦序/爻位范围、上下卦与八卦映射一致性
4. 实现 `ingest-hexagram`：基于 DeepSeek API，将大段文本智能转化为 JSON

**产出**：
- [x] 64 卦 JSON 文件（每卦一文件，如 `01_乾.json` 模板已就绪）
- [x] 数据加载与索引接口（`load_hexagram`、`load_all_hexagrams`、`list_hexagrams`）
- [x] 录入脚本（`add-hexagram`）与校验脚本（`validate-hexagrams`）
- [x] 智能录入（`ingest-hexagram`）：输入大段文本 → DeepSeek 解析 → 输出 JSON

**目录**：`data/hexagrams/`、`scripts/`

**Git**：`feat/phase-2-hexagram-data`

---

### 阶段 3：关联发现（基于重卦结构）

**核心思路**：重卦 = 上卦 + 下卦。卦辞、爻辞的取象遵循固定结构，关联发现应**按结构选取映射**，而非自由语义匹配。

---

#### 一、卦辞：用上下卦映射解释

**规则**：卦辞的意象来自**上卦映射集合**与**下卦映射集合**的组合。

| 输入 | 来源 |
|------|------|
| 上卦名 | `元信息.上下卦.上卦` |
| 下卦名 | `元信息.上下卦.下卦` |
| 上卦映射 | `bagua_mappings[上卦].结构化` → 如「坎为险、为水、为泉」 |
| 下卦映射 | `bagua_mappings[下卦].结构化` → 如「震为雷、为动、为侯」 |

**输出**：从「上卦映射 ∪ 下卦映射」中筛选与卦辞语义相关的项，并生成解释（如「震为动、坎为险 → 动乎险中」）。

**示例**（屯卦）：上坎(险、水) + 下震(雷、动、侯) → 卦辞「动乎险中」「利建侯」。

---

#### 二、爻辞：按爻位与卦象选取映射

**规则 1：爻位与上下卦对应**

| 爻位序 | 所属卦 | 爻位象 |
|--------|--------|--------|
| 1 | 下卦之始 | 初、始、潜、卑下 |
| 2 | 下卦之中 | 中、地面、家中 |
| 3 | 下卦之终 | 极、多凶、人道之始 |
| 4 | 上卦之始 | 入上卦、近君 |
| 5 | 上卦之中 | 君位、尊、中正 |
| 6 | 上卦之终 | 终、亢、极 |

**规则 2：映射来源**

每条爻辞的关联映射主要来自：

1. **本爻所属卦**的映射（爻 1–3 用下卦，爻 4–6 用上卦）
2. **爻位象**（初/二/三/四/五/上的固定含义）
3. **对卦**（可选）：另一卦的映射，用于解释与另一卦的互动
4. **互卦**（可选）：二至四爻、三至五爻构成的卦，用于复杂取象

**规则 3：承乘比应**（若数据中有）：承、乘、应爻关系影响爻辞取象。

---

#### 三、实现流程

```
1. 加载重卦元信息（上卦、下卦）
2. 从 bagua_mappings 取出 上卦、下卦 的映射集合
3. 卦辞：候选 = 上卦映射 ∪ 下卦映射 → LLM 或规则筛选与卦辞相关的项，生成解释
4. 爻辞（逐爻）：确定本爻所属卦 → 候选 = 本卦映射 ∪ 爻位象 ∪ 对卦映射（可选）→ 筛选与解释
5. 输出：{卦爻辞ID: [{映射项, 关联强度, 解释文本}]}
```

---

#### 四、数据约定

| 项目 | 约定 |
|------|------|
| 卦辞 ID | `{卦序}-gc` |
| 爻辞 ID | `{卦序}-{爻位序}` |
| 关联强度 | 0–1，结构匹配优先时可设较高 |
| 映射项格式 | `{卦名}为{象}`，如「乾为龙」「坎为险」 |
| 爻位象 | 可预定义常量表，如 `初位=潜/始`、`五位=君/中` |

---

#### 五、与现有数据的关系

- **卦辞**：不依赖爻辞中的「关联映射」，直接由上卦 + 下卦映射推导
- **爻辞**：可参考 JSON 中的「关联映射」做校验或补全，但不作为唯一来源
- **输出**：写入 `data/associations/associations.json`，格式与现有设计兼容

---

#### 六、技术选型

| 组件 | 选型 | 说明 |
|------|------|------|
| 映射来源 | `bagua_mappings.json` | 八卦映射 |
| 卦爻辞 | `data/hexagrams/*.json` | 含上下卦、爻位等元信息 |
| 解释生成 | DeepSeek API | 对候选映射做筛选与润色 |
| 嵌入模型 | 无需 | 以结构规则替代语义相似度 |

---

#### 七、产出

- 关联发现脚本：按重卦结构选取映射并生成解释
- 关联结果：`data/associations/associations.json`
- 可选：爻位象常量表、互卦计算工具（若需支持互卦取象）

**目录**：`src/association/`、`data/associations/`

**Git**：`feat/phase-3-association-discovery`

---

### 阶段 4：智能体接口

**目标**：提供可被智能体调用的检索与解释接口。

**产出**：
- 检索：输入问题/关键词 → 返回相关卦爻辞
- 解释：输入卦/爻 → 结合映射与关联生成白话解释
- 推荐：输入场景（如「经商风险」）→ 推荐相关卦爻辞

**接口形式**：Python 函数 / 模块 API，后续可封装为工具供 Agent 调用。

**目录建议**：`src/agent/`、`src/retrieval.py`、`src/explain.py`

**Git**：`feat/phase-4-agent-interface`

---

### 阶段 5：质量与迭代（可选）

**目标**：对阶段 3 的关联与解释做质量控制。

**方案**（择一或组合）：
- 人工抽样校验：对关键卦做人工审核
- 用户反馈：使用后标记「有用/无用」，用于迭代
- 置信度标注：为关联打标签（高/中/低），便于过滤

**产出**：校验流程、反馈收集机制、关联置信度字段

**Git**：`feat/phase-5-quality`

---

## 技术选型

| 组件 | 选型 | 说明 |
|------|------|------|
| LLM 调用 | **DeepSeek API** | ingest-hexagram、阶段 3 解释生成 |
| 存储 | **JSON 文件**（推荐） | 见下方存储选型说明 |
| Agent 框架 | 待定 | 阶段 4 集成 |

**ingest-hexagram**：使用 DeepSeek API，需设置 `DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`（可选，默认 https://api.deepseek.com）。可复制 `.env.example` 为 `.env`。

### 存储选型说明：JSON vs SQLite

| 维度 | JSON 文件 | SQLite |
|------|-----------|--------|
| **版本管理** | 文件即数据，`git diff` 可直接看到每次修改的具体内容，便于 code review、回溯、合并 | 数据库为二进制文件，`git diff` 只能看到「文件变了」，无法直观看到改了什么 |
| **查询效率** | 需全量加载到内存，数据量小（如本项目的 8 卦 + 64 卦辞 + 386 爻辞）时几乎无感 | 支持索引、条件查询，适合百万级以上的复杂检索 |
| **读写方式** | 读：`json.load()`；写：整体覆盖或手动解析后局部更新 | 读：SQL；写：INSERT/UPDATE，支持事务、并发 |
| **适用场景** | 数据量小、结构相对固定、希望与代码一起用 Git 管理 | 数据量大、需要复杂查询、多进程/多服务并发写入 |

**本项目建议**：数据量不大（八卦映射 + 卦爻辞 + 关联结果合计约数千条记录），JSON 足够高效，且与代码同仓、同分支，便于版本追溯。若后续关联发现产出大量数据或需要复杂检索，再考虑引入 SQLite。

---

## 开发顺序总结

```
阶段 0（骨架）→ 阶段 1（映射格式）→ 阶段 2（卦爻辞入库）
    → 阶段 3（关联发现）→ 阶段 4（智能体接口）→ 阶段 5（质量，可选）
```

每阶段完成后可打 tag，如 `v0.1-phase1`、`v0.2-phase2`，便于回溯与发布。
