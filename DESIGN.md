# 易经八卦映射智能体 · 设计方案

> 本文档供开发时参考，按阶段划分便于 Git 管理与迭代。

---

## 项目目标

1. **验证**：八卦映射在不同卦爻辞中是否语义一致
2. **挖掘**：卦象组合→卦辞、爻位→爻辞的关联模式
3. **应用**：智能体可检索、解释、推荐卦爻辞

---

## 数据概况

| 类型 | 数量 | 说明 |
|------|------|------|
| 八卦映射 | 8 卦 × 30–50 类/卦 | 已有表格，需转化格式 |
| 卦辞 | 64 条 | 含原文 + 白话 |
| 爻辞 | 386 条 | 含原文 + 白话 |

---

## 阶段划分

### 阶段 0：项目骨架（当前）

**目标**：完成项目初始化，建立基础结构。

**产出**：
- [x] 项目目录、Git 初始化
- [x] uv 包管理（pyproject.toml）
- [x] 本设计文档

**Git**：`feat/phase-0-skeleton`

---

### 阶段 1：映射表 → 智能体可读格式

**目标**：将现有八卦映射表格转化为「结构化 + 自然语言」双形式，便于智能体解析与 LLM 理解。

**产出**：
- 八卦映射的 JSON/YAML 结构化数据（键值对、分类清晰）
- 每卦一段连贯的自然语言描述（供 LLM 直接理解）
- 数据加载与验证脚本

**格式示例**：
```json
{
  "乾": {
    "结构化": {"自然": "天", "人事": "父", "德性": "健", ...},
    "描述": "乾卦象征天、父、刚健，主创生与进取..."
  }
}
```

**目录建议**：`data/bagua_mappings/`

**Git**：`feat/phase-1-mapping-format`

---

### 阶段 2：卦爻辞数据入库

**目标**：将 64 卦辞、386 爻辞以结构化形式存储，与映射数据统一 schema。

**产出**：
- 卦辞 JSON（卦名、卦序、上下卦、原文、白话）
- 爻辞 JSON（爻位、阴阳、当位、原文、白话、吉凶标签）
- 数据加载与索引接口

**目录建议**：`data/hexagrams/`、`data/yao_texts/`

**Git**：`feat/phase-2-hexagram-data`

---

### 阶段 3：关联发现（算法 + LLM）

**目标**：建立「映射 ↔ 卦爻辞」的关联，并生成可成立的解释。

**方案**：
1. **算法层**：嵌入相似度、关键词共现、统计关联 → 产出候选关联
2. **LLM 层**：对候选做「是否成立」「如何解释」的判断与润色
3. **输出**：`{卦爻辞ID, 映射项, 关联强度, 解释文本}`

**产出**：
- 关联发现脚本（可配置：纯算法 / 纯 LLM / 混合）
- 关联结果存储（JSON 或轻量 DB）
- 解释文本生成与缓存

**目录建议**：`src/association/`、`data/associations/`

**Git**：`feat/phase-3-association-discovery`

---

### 阶段 4：智能体接口

**目标**：提供可被智能体调用的检索与解释接口。

**产出**：
- 检索：输入问题/关键词 → 返回相关卦爻辞
- 解释：输入卦/爻 → 结合映射与关联生成白话解释
- 推荐：输入场景（如「经商风险」）→ 推荐相关卦爻辞

**接口形式**：Python 函数 / 模块 API，后续可封装为工具供 Agent 调用。

**目录建议**：`src/agent/`、`src/retrieval.py`、`src/explain.py`

**Git**：`feat/phase-4-agent-interface`

---

### 阶段 5：质量与迭代（可选）

**目标**：对阶段 3 的关联与解释做质量控制。

**方案**（择一或组合）：
- 人工抽样校验：对关键卦做人工审核
- 用户反馈：使用后标记「有用/无用」，用于迭代
- 置信度标注：为关联打标签（高/中/低），便于过滤

**产出**：校验流程、反馈收集机制、关联置信度字段

**Git**：`feat/phase-5-quality`

---

## 技术选型（待定）

| 组件 | 备选 | 说明 |
|------|------|------|
| 嵌入模型 | sentence-transformers / 中文模型 | 阶段 3 语义相似度 |
| LLM 调用 | LiteLLM / 直接 API | 阶段 3 解释生成 |
| 存储 | JSON 文件 / SQLite | 轻量，便于版本管理 |
| Agent 框架 | 待定 | 阶段 4 集成 |

---

## 开发顺序总结

```
阶段 0（骨架）→ 阶段 1（映射格式）→ 阶段 2（卦爻辞入库）
    → 阶段 3（关联发现）→ 阶段 4（智能体接口）→ 阶段 5（质量，可选）
```

每阶段完成后可打 tag，如 `v0.1-phase1`、`v0.2-phase2`，便于回溯与发布。
